# Empty = origin-relative API calls (same host/scheme as the page). This lets
# the app work over both http://62.238.1.221 and the HTTPS Tailscale URL
# without a rebuild, and avoids mixed-content blocking on HTTPS.
VITE_API_URL=
VITE_ENABLE_BRIEFING=false
